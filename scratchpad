<app-navbar></app-navbar>
<div style="display: flex;" [ngStyle]="{'margin-top': '60px'}">
    <mat-card *ngFor="let favoriteMovie of favoriteMovies;" style="flex: 1 1 auto;">

        <mat-card-content>

            <img src={{favoriteMovie.ImagePath}} />

            <mat-card-title [ngStyle]="{'color': '#fff', 'opacity': '0.8', 'margin-top': '8px'}">
                {{favoriteMovie.Title}}
            </mat-card-title>

            <button mat-button [ngStyle]="{'color': '#42a5f5', 'margin-left': '0', 'padding-left': '0'}"
                (click)="showDirector(favoriteMovie.Director.Name, favoriteMovie.Director.Bio, favoriteMovie.Director.Birth)"
                color="primary">
                {{favoriteMovie.Director.Name}}
            </button>

        </mat-card-content>

        <mat-card-actions>
            <div>
                <button mat-flat-button [ngStyle]="{'background-color': '#9575cd', 'width': '100%'}"
                    (click)="showDetails(favoriteMovie.Title, favoriteMovie.Director.Name, favoriteMovie.ImagePath, favoriteMovie.Description)"
                    color="primary">
                    Details
                </button>
            </div>

            <button mat-button [ngStyle]="{'color': '#42a5f5', 'margin-left': '0', 'padding-left': '0'}"
                (click)="showGenre(favoriteMovie.Genre.Name, favoriteMovie.Genre.Description)" color="primary">
                {{favoriteMovie.Genre.Name}}
            </button>

            <mat-icon [ngStyle]="{'color': '#fff176'}" (click)="removeFromFavorites(favoriteMovie._id)">
                favorite
            </mat-icon>

            <!-- <mat-icon *ngIf="!isFavorite(favoriteMovie._id)" [ngStyle]="{'color': '#fff176'}"
                (click)="toggleFavorite(favoriteMovie._id)">
                favorite_border
            </mat-icon> -->
        </mat-card-actions>

    </mat-card>
</div>